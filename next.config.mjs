/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/pro-horica',
  reactStrictMode: true,
  images: {
    unoptimized: true
  }
};

export default nextConfig;
